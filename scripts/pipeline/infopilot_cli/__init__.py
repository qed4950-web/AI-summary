"""Helper modules for the InfoPilot CLI.

Keep `scripts/pipeline/infopilot.py` as the entrypoint, and move reusable CLI
plumbing (policy/session/history) into this package to avoid a monolithic file.
"""

